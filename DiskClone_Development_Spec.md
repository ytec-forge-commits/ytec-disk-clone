# Y-TEC Tsumugi Drive 開発仕様書

**文書版数:** 2.0

**承認日:** 2026年8月4日

**公開方針改定:** 2026年8月20日（Apache License 2.0）

**対象製品:** Y-TEC Tsumugi Drive 1.0.0

**文書区分:** 最上位要件・安全要件・基本設計・ライセンス適合要件

> 本文書はY-TEC Tsumugi Driveの最上位仕様である。v1.xの実装状況、Phase文書、
> ジョブ引継ぎ文書、`.dcimg`／`.dcmig`文書は既存コードと試験証跡を理解するための
> 履歴資料であり、本書と矛盾する場合は本書を優先する。

---

## 1. 目的と完成像

本製品は、Windows 10／11 x64搭載PCの同一PC内ディスク交換、障害復旧、
Windowsディスクおよびデータ専用ディスクのバックアップを、安全側に実行する
Windows／WinPEネイティブツールである。

### 1.1 正式版の必須能力

- Windows版だけで、稼働中Windowsのシステムディスクを別の物理ディスクへ直接クローンできる。
- WinPE版だけで、コピー元とコピー先を直接選択し、クローンを完結できる。
- Windows版とWinPE版の両方で、通常モード、縮小移行モード、MBR維持、MBR→GPTを扱える。
- Windows版とWinPE版の両方で、ディスク／パーティションを`.tsumugi`イメージへ保存できる。
- WinPE版でシステムディスクを復元でき、Windows版でデータ専用ディスクを復元できる。
- 対象ディスクだけを選ぶ通常操作から、Windows、システム領域、起動方式を自動診断して起動修復できる。
- ADK／WinPE不足時に、明示同意後、Microsoft公式配布元から検証済み構成を取得・導入してレスキューメディアを作成できる。
- コピー元保護、安定識別、全書込みの読戻し検証、未完了状態の明示を全経路で維持する。

### 1.2 成果物

1. Windows版ポータブルGUI
2. WinPE版ネイティブGUI
3. 共通クローン、イメージ、縮小移行、起動修復エンジン
4. ADK／WinPE導入・レスキューメディア作成ウィザード
5. 読取り専用診断CLIおよび開発・試験CLI
6. `.tsumugi` v1形式仕様と後方互換試験データ
7. Portable ZIP、操作マニュアル、Web説明、アプリ内ヘルプ
8. 利用規約、プライバシー説明、第三者通知、SPDX SBOM、SHA-256一覧
9. VM・代表実機の試験仕様と結果

### 1.3 明示的な非目標

- 異なるPCへのWindows移植保証
- GPT→MBR変換
- 差分／増分／スケジュールバックアップ
- クラウド保存、ネットワーク共有へのイメージ保存、テレメトリ
- 独自カーネル／ファイルシステム／変更ブロック追跡ドライバー
- BitLocker、Windowsライセンス認証、Secure Boot、資格情報の回避
- 動的ディスク、Storage Spaces、WindowsソフトウェアRAID、不明確なハードウェアRAID
- 製品機能からのVHD／VHDX直接操作
- ARM64／x86アプリ、Windows Serverの正式サポート
- 完成WinPE、ADK、Microsoft標準ツールの再配布

---

## 2. 仕様の優先順位と旧資産の扱い

### 2.1 規範文書

1. 本書
2. `AGENTS.md`
3. `docs/safety-model.md`
4. `docs/architecture.md`
5. `.tsumugi` v1の形式仕様
6. 個別の実装・テスト計画

`AGENTS.md`は本書の承認済み限定例外と矛盾しないよう追随更新する。安全条件を
弱める方向に読める場合は、より厳しい条件を採用して人間へ確認する。
同名の`DiskClone_Development_Spec.docx`はv1時点の履歴スナップショットであり、
v2の規範文書ではない。v2へ再生成・検証するまで正式配布物へ含めない。

### 2.2 予約ジョブ方式の完全廃止

- Windows版の予約ジョブ作成、PEへの予約、ジョブ一覧、固定名自動検索、auto-once、claim、結果取込みを廃止する。
- WinPE版の予約ジョブ選択・自動実行を廃止する。
- 公開CLIの`--job-preflight`、`--job-execute`その他`--job-*`を正式版から削除する。
- クローン、復元、MBR→GPT、起動修復は、同じ実行セッション内で選択、確認、実行、検証する。
- 旧ジョブファイルは検索、読込み、変換、実行、移動、上書き、削除しない。利用者の既存ファイルとして完全に放置する。
- `docs/job-handoff-v2.md`と過去のジョブ試験証跡は履歴資料として保持するが、正式版の公開UI・CLI仕様ではない。

### 2.3 旧イメージ形式

- `.dcimg`／`.dcmig`は正式版UIから開けない。
- 旧形式のパーサーは、既存試験から必要な安全部品を`.tsumugi`へ移行する間だけ内部に保持できる。
- 旧形式を新形式へ変換する公開機能は提供しない。
- `.tsumugi` v1正式化後、旧形式の製品コードを段階的に削除する。

---

## 3. 最重要安全原則

優先順位は次のとおりとする。

1. コピー元のデータと暗号化状態をアプリが変更しない。
2. 誤ったコピー先、復元先、USB、起動修復先を変更しない。
3. 破損・差替え・未検証イメージを復元しない。
4. 未完了、欠損、起動未確認の結果を成功扱いしない。
5. ライセンス違反または未監査の配布物を生成しない。
6. その範囲内で速度と操作性を最適化する。

### 3.1 統一実行契約

全ての破壊的操作を次の契約へ統一する。

```text
OperationPlan
  -> read-only preflight
  -> source/target session open
  -> stable identity revalidation
  -> target summary + uppercase OK
  -> target invalidation
  -> execute
  -> flush + read-back verification
  -> final metadata commit
  -> OperationResult
```

- `OperationPlan`は選択内容、対象識別、レイアウト、変換、必要容量、危険、確認対象を不変値として保持する。
- 確認後も実行直前、offline／online変更後、ハンドル取得後に対象を再識別する。
- ディスク番号、ドライブ文字、表示順だけを同一性判断に使わない。
- 書込みは対象専用Writerだけに隔離し、コピー元型は書込みメソッドを持たない。
- パーティション表を先に無効化し、データとメタデータの検証後、完成を示す先頭メタデータを最後に確定する。

### 3.2 コピー元無変更の意味

- **Windows版:** 既定ではアプリのクローン／イメージエンジンはコピー元物理ディスク、ボリューム、BCDへ直接書き込まない。稼働中WindowsおよびVSSによる通常の書込みは発生する。結果は開始時点Snapshotであり、物理的な完全無書込みを保証しない。
- Windows版の詳細設定で、選択対象外の別パーティションを同じ物理ディスク上のイメージ保存先にする場合だけ例外とする。この操作は保存先パーティションへ明示的に書き込み、ディスク故障対策にならず、ディスク全体イメージには使えないことを強く警告する。
- **WinPE版:** コピー元をOSレベルでも読取り専用に固定し、アプリとOSの双方からコピー元へ書き込まない。
- 完全無変更が必要な利用者にはWinPE実行を推奨する。
- EXEまたはデータフォルダーがコピー元物理ディスク上にある場合、処理中ログと中断情報をRAM、コピー先、イメージ保存先、またはレスキューUSBへ置く。

### 3.3 破壊確認

- 対象のモデル、容量、接続方式、シリアル末尾、GPT／MBR、パーティション図、消去／変更内容を表示する。
- 全ての破壊操作は、表示内容を確認後に大文字`OK`の完全一致だけで承認する。
- クローン先消去、イメージ復元、USB初期化、起動修復で同じ原則を使用する。
- `OK`を省略、自動入力、記憶、コマンドライン固定値で回避できる製品経路を作らない。

### 3.4 中止・電源・健康状態

- 手動の一時停止、再開、安全な取消をWindows版とWinPE版へ実装する。
- 最終メタデータ確定中など取消不能区間を明示する。
- Windows上ではVSS差分領域を監視し、危険になった場合は再開または安全な取消を要求する。
- 実行中の自動スリープを防止する。完了後動作は「何もしない／スリープ／再起動／シャットダウン」から環境に応じた有効項目だけを選ばせ、既定は「何もしない」とする。
- ノートPCはAC接続を推奨し、バッテリー50%未満では追加確認する。
- 温度は表示と警告だけを行い、自動一時停止しない。装置閾値を優先し、不明なら「取得不可」と表示する。
- コピー先のSMART／NVMe重大異常は開始禁止。コピー元異常は救出モードを推奨し、明示確認後に続行可能とする。

---

## 4. 対応範囲

### 4.1 対応レベル

- **正式対応:** リリース試験表の必須条件で合格し、通常利用を案内できる。
- **条件付き対応:** 安全ゲートを実装済みだが、試験機種・構成が限定される。条件と未試験範囲を明示する。
- **未保証:** 既定では詳細機能として隠し、警告と条件確認後に試行できるが、正式な成功保証をしない。
- **非対応:** 誤処理を避けるため開始しない。

### 4.2 OSと起動方式

| 対象 | レベル | 条件 |
|---|---|---|
| Windows 10 22H2 x64 | 正式対応 | サポート終了済みであることを表示 |
| Windows 11 24H2／25H2 x64 | 正式対応 | ADK 10.1.26100.2454の公式対象。リリースごとのVM回帰対象 |
| Windows 11 26H1 | 非対応 | Microsoft公式ADKは26H1 Arm64向け。x64アプリ／x64 PEの本製品では正式対応を表示しない |
| UEFI／GPT | 正式対応 | Windows 10／11 x64 |
| Legacy BIOS／MBR | 正式対応 | Windows 10／11 x64 |
| Windows 7／8系ディスク | 未保証 | WinPEまたは対応Windowsホストから一度警告して試行可能 |
| ARM64／32bit Windowsアプリ | 非対応 | ディスク診断結果は推測で補完しない |

Windows 7／8系上でWindows版アプリを起動することは保証しない。対象ディスクを
WinPEまたは正式対応Windowsへ接続して扱う。

### 4.3 ディスクとセクター

- SATA HDD／SSD、NVMe SSD、USB接続されたSATA／NVMeディスクを正式対応とする。
- USBメモリはクローン先・復元先から除外する。レスキューメディア用途だけに使用する。
- 一意シリアルを持たないUSB筐体は、同一接続中だけパス／ポート／モデル／容量／レイアウトの複合識別で条件付き対応する。切断を検知した時点で計画を無効化し、再選択と`OK`を要求する。
- 同じ論理セクターサイズの通常クローンを正式経路とする。
- 512系↔4Knはファイル単位／縮小移行だけを許可する。
- 4Knは物理媒体試験が十分でない間、条件付き対応と表示する。

### 4.4 パーティションとファイルシステム

- GPT→GPT、MBR→MBR、MBR→GPTを対応する。GPT→MBRは非対応とする。
- NTFS、exFAT、FAT32は通常コピー、イメージ、縮小移行の対象とする。
- MSRは定義だけを再作成する。
- 対応外ファイルシステムを含む混在ディスクでは、そのパーティションを元サイズのRAWとしてコピーできる。
- RAWコピーしたLinux等の内容はバイト保存だけを対象とし、GRUB、UUID、同時接続、起動を保証しない。
- 動的ディスク、LDM、Storage Spaces、ソフトウェアRAID、解析不能レイアウトは停止する。

### 4.5 BitLocker

- BitLockerは1.0.0では**未保証の詳細機能**とする。
- コピー元を完全復号するための書込みは行わない。
- Windows版は既にUnlockされたボリュームをVSS／ファイル単位で読み取る。
- WinPE版は利用者が回復キーまたはパスワードを入力した場合だけ一時Unlockする。
- 回復キー、パスワードを保存、ログ、送信、解析、回避しない。
- コピー先は暗号化されていない状態で完成させ、再暗号化は利用者判断と明示する。

---

## 5. 直接クローン

### 5.1 共通機能

- コピー元とコピー先を同じ画面フローで直接選択する。
- コピー先はRAWに限定せず、NTFS、exFAT、FAT32、既存GPT／MBR基本ディスクも候補に表示し、確認後に全体を消去・再構成する。
- 通常モード／縮小移行モードを自動推奨し、利用者が安全条件内で上書き選択できる。
- MBR元でUEFI対応Windowsを確認できた場合はMBR→GPTを推奨し、MBR維持も選択可能にする。
- コピーするパーティションを選択可能にし、既定は全選択とする。
- Windowsを選択した場合はESP／システム／MSR／必要な回復領域を自動選択し、解除不可にする。
- 余剰容量は「自動配分」を既定とし、「未割当のまま」も選択可能にする。複数対象は最小必要量確保後、元サイズ比で配分する。
- exFAT／FAT32データ領域は形式維持を既定とし、詳細設定でNTFS変換を選べる。
- 旧ドライブ文字の予約は一度限りの任意設定とし、元ディスクを外した後に適用する。

### 5.2 Windows直接クローン

- Windows版アプリは起動時に管理者権限を要求し、処理途中で追加UACを要求しない。
- 稼働中システムディスクの全対象ボリュームを同一VSS Snapshot setへ追加する。
- NTFS等はSnapshotから読み、Snapshot対象外の静的システム領域だけを読取り専用物理Readerから取得する。
- Snapshotと物理領域の対応、Geometry、ブートセクター、役割が一意でなければ停止してWinPEを案内する。
- VSS Writer異常は既定で停止する。詳細設定で明示した場合だけクラッシュ整合性Snapshotを許可し、その事実を結果へ残す。
- MBR→GPTを含め、コピー先側のレイアウト作成、内容移行、BCD／WinRE処理までWindows上で完了する。
- 完了したシステムクローン先はofflineのまま保持し、「検証完了・換装待ち」と表示する。
- 結果へSnapshot作成UTCを記録し、利用者へ「開始時点の状態」であることを表示する。

### 5.3 WinPE直接クローン

- WinPEの初期画面から「ドライブをクローン」を選択し、コピー元、コピー先、方式をその場で指定する。
- Windows版と同じ通常／縮小、MBR維持、MBR→GPT、パーティション選択を提供する。
- コピー元を読取り専用に固定してから対象計画を確定する。
- 固定名ジョブ、ジョブファイル、Windows側の予約を要求しない。

### 5.4 移行後チェック

換装後にWindows版から任意の読取り専用チェックを実行できる。

- 現在の起動ディスクと想定レイアウト
- BCD／Windows Boot Manager
- WinRE登録と回復領域
- ファイルシステムとディスク健康状態
- BitLocker状態

これは実際の事前起動成功を保証する機能ではなく、換装後の診断である。

---

## 6. `.tsumugi` v1イメージ

### 6.1 形式方針

- 正式拡張子は`.tsumugi`とする。
- 1つの通常ファイルに、通常RAW、縮小用ファイルペイロード、パーティション選択、暗号化、不良セクターマップを格納する。
- NTFSまたはexFAT上の単一ファイルだけを正式対応とし、FAT32向け4GiB分割は実装しない。
- 形式v1の読み取り互換性を今後の製品版で恒久的に維持する。
- 未知の必須機能フラグ、未知の暗号、未知の圧縮、範囲外データを拒否する。

### 6.2 論理構造

```text
[bounded public header]
[encrypted-or-plain manifest]
[partition/layout records]
[chunk index]
[chunk payloads]
[bad-range map]
[verification footer]
```

- 公開ヘッダーはマジック、形式版、必須機能フラグ、KDF／暗号に必要な最小メタデータだけを持つ。
- 暗号化時はディスク名、パーティション情報、内容索引を含むマニフェストを暗号化する。
- 全ての位置、長さ、個数、加算、乗算、重複、正規順序を有界検証する。
- チャンクは既定16MiB以下とし、ゼロ、非圧縮、Zstandard圧縮、ファイルペイロードを明示する。
- 圧縮は自動とし、小さくならないチャンクは非圧縮へ戻す。利用者向け圧縮設定は設けない。
- 復元結果に影響する仕様値はヘッダー／マニフェストへ版付きで記録する。

### 6.3 暗号化

- 暗号化は任意で、既定OFFとする。
- KDFはArgon2公式参照実装20190702のArgon2idを使用する。
- v1既定パラメーターはメモリ64MiB、反復3、並列度1、イメージごとの16バイトSaltとし、実値をヘッダーへ記録する。
- 内容暗号はWindows CNG AES-256-GCMとし、チャンクごとに一意Nonceと認証Tagを持つ。
- 鍵素材とパスワードバッファは使用後に明示消去する。
- パスワードは8文字以上のASCII印字文字とし、確認入力、押して表示、Caps Lock／配列状態を提供する。
- 弱いパスワードは警告するが続行可能とする。回復キーは設けず、紛失時は復元不能と表示する。

### 6.4 作成と置換

- 既定名は`Tsumugi-Image_YYYYMMDD-HHMMSS.tsumugi`とし、PC名を含めない。
- 同名の完成イメージを置き換える場合は確認を取り、同じフォルダーの新規`.partial`へ作成する。
- `.partial`をflushし、選択された検証を終え、完成メタデータを確定してから回復可能な入替えを行う。旧完成ファイルを先にtruncate／削除しない。
- 置換には旧＋新の両方を保持できる空き容量を要求する。
- 作成時検証は「完全」を既定とし、「高速」も選択できる。高速でも各書込みの即時読戻し、認証／ハッシュ、最終メタデータ検証を省略せず、完成後の追加全走査だけを省く。
- 復元時は作成設定に関係なく、書込み／削除共有なしの同一ファイルハンドルで全体を完全検証してから対象へ書き込む。
- 既定ではイメージ保存先をコピー元物理ディスクから分離する。Windows版の詳細設定だけは、イメージ対象から除外した別パーティションを同一物理ディスク上の保存先にできる。毎回強い警告を表示し、ディスク全体選択、保存先パーティション選択、PEの読取り専用ソースでは許可しない。

### 6.5 復元

- ディスク全体復元と個別パーティション復元を提供する。
- 個別復元先は既存パーティションまたは未割当領域とする。
- 復元先の既存内容は自動バックアップしない。パーティション表の退避可否と、上書き内容が回復不能であることを表示する。
- 元の物理ディスク自身へ戻す操作も、対象要約と`OK`の後に許可する。
- Windowsパーティションだけを復元した場合は、起動修復計画を提示し、同意時だけ実行する。
- Windows版は現在起動中のシステムディスク復元を拒否し、PEでイメージと対象を改めて選択するよう案内する。ジョブや隠れた引継ぎは作成しない。

---

## 7. 単一中断チェックポイント

- 予約ジョブとは独立した1件の中断チェックポイントだけを保持する。
- 利用者には「前回の中断処理を再開／破棄」として表示し、ジョブ一覧や予約として表示しない。
- 処理種別、確定した安定識別、Snapshot識別、レイアウトHash、検証済みチャンク、出力ファイル識別を保持する。
- 同じSnapshot、同じソース状態、同じ出力、同じ対象接続を証明できる場合だけ再開する。
- 再開前に既存チャンクを再検証する。証明できない場合は再開を拒否し、最初からやり直す。
- 「破棄」はアプリが所有する中断チェックポイントと未完成出力だけを対象にする。旧ジョブファイルや利用者ファイルを削除しない。

---

## 8. 救出モード

- 標準モードは読取りエラーで停止する。
- 救出モードは明示選択し、前方、逆方向、小ブロック、有限再試行で読取り可能範囲を優先する。
- 読めない範囲はゼロ埋めし、正確な範囲と試行結果を欠損マップへ記録する。
- 救出結果は常に「一部欠損」と表示し、通常成功と同じ表示・終了状態にしない。
- 救出時は縮小、ファイルシステム変換、MBR→GPTを行わない。
- RAWクローンは同容量以上、救出イメージは`.tsumugi`へ保存する。
- システムディスク救出はWinPE限定。Windows版は非システムデータディスクだけを扱う。
- GPL系救出ツールを同梱せず、既存のネイティブI/O境界上に実装する。

---

## 9. 起動修復

### 9.1 通常フロー

1. 利用者が対象ディスクを選択する。
2. アプリがWindowsインストール、GPT／MBR、ESP／Active領域、WinRE、第三者EFIローダーを読取り専用診断する。
3. 複数Windowsがある場合、全登録＋優先順位、または選択したWindowsだけを選ばせる。
4. 修復項目、書込み対象、危険、退避先、NVRAM変更有無を表示する。
5. 大文字`OK`の確認後、対象を再識別して必要な修復だけを実行する。
6. BCD／起動ファイル／属性／WinREを再検証し、完全、部分修復、失敗を区別する。

### 9.2 修復範囲

- Microsoft署名済みBCDBootによるBCD新規再構築
- 起動ファイル、ESP、Active属性、BIOSブートコード、UEFIフォールバック
- 安全に識別できる`Winre.wim`のWinRE登録
- 必要なESP／システム領域がない場合の、安全に縮小可能なNTFSからの新規作成

既存BCDの退避、新規BCD確認、失敗時ロールバックを1トランザクションにする。
第三者EFIローダーを検出した場合は保持／削除を選択させる。

### 9.3 NVRAMと他PC向けディスク

- 「このPCで使用する」と利用者が明示した場合だけ、Windows Boot ManagerのUEFI NVRAM登録を修復する。
- 既存ブート順序を不必要に変更しない。
- 他PC向けディスクではESP内だけを修復し、現在PCのNVRAMを変更しない。
- WinREを安全に特定できない場合は通常起動だけを修復し、「一部修復」と報告する。

---

## 10. レスキューメディア

### 10.1 Microsoft配布物

- 製品ZIP、Gitリポジトリ、Y-TECサイトへADK／WinPE／WIM／ISO／CAB／Microsoft標準EXE・DLLを同梱しない。
- 製品リリースごとに試験済みADK、WinPE Add-on、Servicing Update、公式URL、署名者、版、SHA-256を許可リストへ固定する。
- 未検証の「最新版」をWebページ解析で自動選択しない。

### 10.2 一括導入

ADK／WinPE不足時は次の順で処理する。

1. 必要製品、容量、Microsoft公式URL、利用条件へのリンクを表示する。
2. 利用者が明示的に続行へ同意する。
3. Microsoft公式HTTPS URLから固定インストーラーを一時領域へ取得する。
4. Authenticode署名者、製品版、固定SHA-256を検証する。
5. MicrosoftがサポートするquietオプションでDeployment ToolsとWinPE Add-onを導入する。
6. 必須Servicing Updateを導入し、最終ファイルの署名と版を再検査する。
7. メディア作成を続行する。
8. 成功後に一時ダウンロードを削除する。導入済みADKは保持する。

アプリがEULA同意を代行・偽装してはならない。設定画面には、この機能で導入した
構成を識別してMicrosoftの通常アンインストールを呼ぶ安全な削除機能を用意する。
利用者指定先へMicrosoft公式`/layout`を作成・検証し、オフラインPCで使用する機能も提供する。

### 10.3 USB／ISO

- USBは8GiB以上を必須、16GiB以上を推奨する。
- 既定構成は4GiB FAT32起動領域＋残容量のNTFSデータ領域とし、データ領域はexFATも選択できる。
- 初回または未知USBはディスク全体を`OK`確認後に初期化する。
- 検証済みY-TECレスキューUSBは、データ領域を保持して起動／アプリ領域だけ更新できる。
- 起動中USBの物理ディスク全体をクローン／復元／救出の書込み対象から除外する。データ領域はイメージ・ログ保存先として使用できる。
- Secure Boot 2011 CA互換／2023 CAを現在PCと用途から推奨し、詳細設定で変更可能にする。
- 現PCから必要な署名済みx64ストレージ／USBドライバーだけを収集して一覧表示する。他PC向けはメーカーINFフォルダーを任意指定できる。
- PEの詳細メニューに警告付きコマンドプロンプトを置く。通常操作はGUIだけで完結させる。

---

## 11. UI、保存、診断

### 11.1 UI

- 製品名は「Y-TEC Tsumugi Drive」とし、既存の三本糸モチーフをEXE、PE、Web、PDFで統一する。
- 初期画面は操作可能な左ナビゲーションとし、クローン、イメージ作成、イメージ復元、起動修復、レスキューメディア、ログ・診断を配置する。
- 各操作は3～4段階の短いウィザードとし、危険な詳細設定は折りたたむ。
- 初回起動時に、対象確認、元ディスク保護、検証結果の意味の3項目だけを案内し、再表示可能にする。
- ディスクカードへパーティション図、モデル、容量、Bus、GPT／MBR、シリアル末尾、Windows／データ／USB、健康状態を表示する。
- 1280×720を基準とし、1024×600ではスクロールして全機能を操作できるようにする。
- 125～200% DPI、Tab、Enter、Esc、見えるフォーカス、色以外の状態表示を必須とする。
- 主ボタンを可変幅で再配置し、文字切れ、重なり、クリック不能を禁止する。
- エラーは短い日本語要約、エラーコード、次の操作、折りたたみ・コピー可能な詳細で表示し、巨大なスタックトレースを主ダイアログへ出さない。

### 11.2 ポータブル保存

- 配布はPortable ZIPだけとし、設定とログをEXE隣の`data`へ保存する。
- AppDataへ保存・自動退避しない。EXEフォルダーが書込み不能なら読取り専用診断だけを許可する。
- アップデート時は`data`を保持する。
- 通常ログは30日または合計200MiB、失敗ログは最長90日で循環する。
- PEログはレスキューUSBデータ領域へ自動保存し、不可能なら代替先を選ばせる。
- `.tsumugi`と`.partial`を`data`へ保存しない。

### 11.3 プライバシーと通信

- テレメトリ、広告、クラウド同期、クラッシュ自動送信を実装しない。
- 製品ネットワーク通信は、利用者が開始したMicrosoft公式ADK取得と、Y-TECサイトへの手動更新確認だけに限定する。
- 更新確認は自動実行せず、利用者が「更新を確認」を押した場合だけ固定HTTPS URLへ接続する。
- ログのシリアルは末尾＋不可逆Hash、パスは最小化し、パスワード、回復キー、文書名を記録しない。
- サポートZIPは追加マスク後、含有ファイル一覧を表示してローカル保存する。自動送信しない。

---

## 12. 依存・ライセンス

### 12.1 承認済み依存

| 名称 | 固定版 | ライセンス | 用途 |
|---|---|---|---|
| Zstandard | 1.5.7 | BSD-3-Clause | `.tsumugi`チャンク圧縮 |
| LINE Seed JP | LINESeedJP_20241105 | OFL-1.1 | Windows／WinPE日本語UI |
| Argon2 reference implementation | 20190702 | Apache-2.0 | Argon2id KDF、静的ソース組込み |

Argon2の追加は2026年8月4日に人間が正式承認した。採用時は公式配布物のHash、
ライセンス本文、NOTICE要否、静的リンク、改変内容を依存台帳、第三者通知、SBOMへ記録する。

### 12.2 禁止・審査対象

- GPL、AGPL、SSPL、LGPL、MPL、EPL、Commons Clause、BSL、独自非商用、ライセンス不明の依存を製品へ組み込まない。
- 新規依存は名称、版、公式配布元、ライセンス、用途、リンク方式、配布物への含有、代替、戻し方を提示し、人間の承認後に追加する。
- 競合製品のコード、文言、UI、独自形式を模倣しない。

### 12.3 Microsoftと製品ライセンス

- Microsoft標準ツールは対象Windows／WinPEの正規System32から署名検証後に呼び、製品へコピーしない。
- Visual Studio／Build Toolsの使用資格と、静的MSVCランタイムの配布条件をリリースごとに記録する。
- Y-TECが著作権を持つソース、文書、ビルド成果物はApache License 2.0で公開し、同ライセンス条件に従う利用、改変、再配布を許可する。公式Y-TECビルドと第三者ビルドは商標・サポート上明確に区別する。
- Windowsその他のライセンスを本製品が付与・移転しないこと、認証回避を提供しないことを製品内と利用規約へ明記する。

---

## 13. 検証とリリース

### 13.1 自動・合成試験

- GPT／MBR、安定識別、容量／オフセット、縮小配置、パーティション選択
- `OperationPlan`不変性、再識別、`OK`確認、取消／commit境界
- `.tsumugi`ヘッダー、暗号、認証Tag、チャンク、圧縮、全体検証、旧版読取り
- 不正長、整数オーバーフロー、重複、切詰め、未知フラグ、破損、Nonce再利用拒否
- Argon2id固定ベクター、AES-GCM改ざん、鍵消去境界
- 中断再開の同一性、差替え拒否、破棄対象限定
- VSS Writer、Snapshot cleanup、差分領域、クラッシュ整合性明示
- 救出モードの有限再試行、欠損マップ、成功誤表示防止
- 起動修復計画、BCD退避／復元、NVRAM分岐、第三者EFI保持
- ADK URL／署名／Hash／版、offline layout、USB領域保持
- ビルド、CTest、静的解析、ASan、Fuzz、ライセンス、安全境界、SBOM

### 13.2 VM試験

- VMラボはNIC無効、合成データだけ、1台ずつ使用する。
- Windows 10 22H2 x64、Windows 11 24H2／25H2 x64を正式マトリクスとする。Windows 11 26H1は選定新規デバイス向けArm64系統であり、x64アプリ／x64 PEの本製品では非対応とする。
- UEFI／GPT、Legacy BIOS／MBR、MBR→GPT、通常、縮小、データ専用、暗号化、救出、中断再開、破損、取消、起動修復を再実行する。
- Windows直接システムクローンはコピー元を外した対象単独起動まで確認する。
- WinPEはジョブなしで各操作を直接選択し、単独で完結することを確認する。
- ADK未導入のクリーンVMで、同意、公式取得、署名検証、導入、更新、ISO、USB相当、アンインストール、offline layoutを確認する。
- 既存v1ジョブ経路の過去PASSは部品証跡としてのみ利用し、v2製品経路の合格に代用しない。

### 13.3 UI・配布監査

- 1280×720、1024×600、125／150／175／200% DPIで実画面を確認する。
- Tab順、Enter／Esc、フォーカス、スクロール、長い日本語、主ボタン、エラー詳細を確認する。
- Portable ZIPにMicrosoft媒体、デバッグランタイム、旧ジョブ見本、秘密情報がないことを検査する。
- ZIP内のSBOM、第三者通知、ライセンス、利用規約、プライバシー、マニュアル、SHA-256を一致させる。

### 13.4 実機と公式バイナリ公開

- ソースと非物理試験の証跡は公開GitHub Organization
  `ytec-forge-commits/ytec-disk-clone`で継続公開する。開発版ソースの公開を、代表実機合格済み
  または正式バイナリ公開済みとは表現しない。
- 代表実機でWindows直接システムクローン、縮小SSD移行、PE直接クローン、データディスクのイメージ作成／復元、USB起動、移行後チェックを実施する。
- 未試験ハードウェアは条件付き対応または未保証とし、正式対応へ誇張しない。
- 正式版は`1.0.0`、未署名Portable ZIPとし、Unknown Publisher／SmartScreenの説明とSHA-256を掲載する。
- Y-TECが品質保証する公式バイナリの公開先はY-TECサイト
  `/ytb/tsumugi-drive/`だけとし、当面GitHub Releasesへ公式バイナリを置かない。
  第三者によるApache-2.0準拠の再配布を制限しないが、公式版と誤認させない。
- 公式バイナリの公開ベータは行わず、内部検証と代表実機試験後に正式版を公開する。
- 公開ページはデスクトップ幅と375pxで確認し、既存サポートフォーム`/ytb/request-form/`へ案内する。
- 名称はJ-PlatPat等の公的DBと一般Webで調査し、専門家確認ではない限界を記録する。利用規約、商標、Visual Studio使用資格を人間が最終確認する。

### 13.5 リリース停止条件

以下のいずれかがあれば一般公開しない。

1. 予約ジョブ経路または`--job-*`が正式版UI／CLIから到達可能。
2. Windows直接クローンまたはPE直接操作が未接続。
3. コピー元へのアプリ書込み経路、誤対象を止める再識別不足、読戻し省略がある。
4. `.tsumugi`の不正入力、暗号認証失敗、完全検証失敗後に対象書込みへ進める。
5. 旧ジョブファイルを自動検索、読込み、変更、削除する。
6. Microsoft製媒体・ツールを製品または公開サーバーから再配布する。
7. ADK取得先、署名、版、Hashが固定・検証されない。
8. SBOM、第三者通知、依存台帳、実配布物が一致しない。
9. 禁止ライセンス、使用資格不明のツールチェーン、秘密情報が含まれる。
10. 代表実機試験、公開ZIP監査、公開ページとのHash一致が未完了。

---

## 14. 変更管理

次の変更には版更新、人間承認、影響・移行・試験・ライセンス記録が必要である。

- 対応OS、ファイルシステム、容量、セクター、BitLocker方針
- コピー元書込み境界、確認語、再識別、検証省略
- MBR→GPT、起動修復、救出モード
- `.tsumugi`形式、暗号、KDF、圧縮、後方互換
- Microsoft取得／配布方式、外部依存、ライセンス
- ネットワーク、テレメトリ、クラウド、カーネルドライバー
- 正式／条件付き／未保証／非対応の区分
- リリース停止条件

## 15. 参考資料

以下は2026年8月4日時点の実装・配布判断の基礎資料である。内容、URL、利用条件は
変更され得るため、正式公開時とADK許可リスト更新時に再確認する。

- [Microsoft Learn: Download and install the Windows ADK](https://learn.microsoft.com/en-us/windows-hardware/get-started/adk-install)
- [Microsoft Learn: Install the Windows ADK offline](https://learn.microsoft.com/en-us/windows-hardware/get-started/adk-offline-install)
- [Microsoft Learn: Windows PE overview](https://learn.microsoft.com/en-us/windows-hardware/manufacture/desktop/winpe-intro?view=windows-11)
- [Microsoft Learn: Volume Shadow Copy Service](https://learn.microsoft.com/en-us/windows-server/storage/file-server/volume-shadow-copy-service)
- [Microsoft Learn: Windows 11 release information](https://learn.microsoft.com/en-us/windows/release-health/windows11-release-information)
- [Microsoft Learn: Windows 11, version 26H1 known issues and notifications](https://learn.microsoft.com/en-us/windows/release-health/status-windows-11-26h1)
- [Microsoft Learn: Download and Install the Windows ADK Patches](https://learn.microsoft.com/en-us/windows-hardware/get-started/adk-servicing)
- [Microsoft Learn: Windows 10 release information](https://learn.microsoft.com/en-us/windows/release-health/release-information)
- [Microsoft Learn: Redistributing Visual C++ files](https://learn.microsoft.com/en-us/cpp/windows/redistributing-visual-cpp-files?view=msvc-170)
- [Visual Studio Community](https://visualstudio.microsoft.com/vs/community/)

## 16. v2.0変更履歴

- 製品予約ジョブとPE引継ぎを廃止し、Windows／PEの直接実行へ統一した。
- 旧ジョブファイルを一切探索・変更しない方針を追加した。
- Windows稼働中システムクローンをVSS Snapshotで正式要件化した。
- 通常／縮小、Windows／データ専用、パーティション選択を統合した。
- 正式イメージ形式を単一ファイル`.tsumugi` v1へ変更した。
- Argon2id＋AES-256-GCM暗号化と、単一中断チェックポイントを承認した。
- 標準停止とは別に、欠損を明示する救出モードを追加した。
- 対象ディスクだけを選ぶ自動起動修復へ拡張した。
- Microsoft公式ADKの明示同意付き自動取得・検証・導入を限定通信例外として承認した。
- 対応範囲を正式対応、条件付き対応、未保証、非対応へ分類した。
